# =============================================================================
# Fynd Review API - Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# For Docker Compose (automatically configured):
# DATABASE_URL=postgresql://fynd:fynd_password@postgres:5432/fynd_reviews

# For local PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/fynd_reviews

# For cloud providers:
# Neon: postgresql://user:password@host.neon.tech:5432/neondb
# Supabase: postgresql://postgres:password@db.projectid.supabase.co:5432/postgres
# Render: Use DATABASE_URL provided in dashboard
DATABASE_URL=postgresql://user:password@localhost:5432/fynd_reviews

# Docker Compose PostgreSQL settings (only used with docker-compose)
POSTGRES_USER=fynd
POSTGRES_PASSWORD=fynd_password
POSTGRES_DB=fynd_reviews
POSTGRES_PORT=5432

# -----------------------------------------------------------------------------
# LLM Configuration
# -----------------------------------------------------------------------------
# LLM API Key (Required)
# OpenAI: Get from https://platform.openai.com/api-keys
# Gemini: Get from https://makersuite.google.com/app/apikey
# OpenRouter: Get from https://openrouter.ai/keys
LLM_API_KEY=your-api-key-here

# LLM Provider (openai, gemini, openrouter)
LLM_PROVIDER=openai

# Optional: Override default model
# OpenAI options: gpt-3.5-turbo, gpt-4, gpt-4o-mini
# Gemini options: gemini-pro, gemini-1.5-flash
# OpenRouter: any supported model
# LLM_MODEL=gpt-3.5-turbo

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
HOST=0.0.0.0
PORT=8000
API_PORT=8000

# -----------------------------------------------------------------------------
# Development Settings
# -----------------------------------------------------------------------------
# Uncomment for debug mode
# DEBUG=true
